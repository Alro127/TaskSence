# Run Record

## Run Meta

- Run ID: RUN-2026-06-05-005
- Date: 2026-06-05
- Trigger: User reported Docker docs described Nginx incorrectly compared with the original Nginx config.
- Request Summary: Update Docker documentation to match `proxy/nginx.conf` without editing Docker Compose.
- Primary Skill(s): Documentation, infra review.
- Agent Role(s): Coding/documentation agent

## Inputs Snapshot

- Key files reviewed:
  - `proxy/nginx.conf`
  - `proxy/Dockerfile`
  - `infra/docker/compose.yaml`
  - `infra/docker/compose.build.yaml`
  - `.env.example`
  - `docs/DOCKER_*.md`
- Constraints detected:
  - User explicitly requested not to edit Docker Compose.
  - Current Nginx listens only on port `80`.
  - Current public Nginx routes are `/`, `/api/core/v1`, and `/api/chat/v1`.
  - Nginx does not expose `/health`, `/api/v1`, `/ai`, `/chat`, or HTTPS `443`.
  - `proxy/Dockerfile` copies `proxy/nginx.conf` into the image; compose does not bind mount it.
- Assumptions made:
  - Docker docs should use `infra/docker/compose.yaml` as the command source of truth.
  - Internal Spring/AI `/api/v1` references are valid only as upstream targets behind Nginx.

## Plan and Validation

- Planning approach:
  - Replace stale Docker docs with concise docs aligned to current compose and Nginx files.
  - Keep public route documentation explicit so operators do not call obsolete paths.
  - Preserve the no-compose-edit constraint.
- Plan reviewer findings: N/A.
- Design compliance checks: N/A; documentation-only change.

## Work Completed

- Changes made:
  - Rewrote `docs/DOCKER_SUMMARY.md`, `docs/DOCKER_SETUP.md`, `docs/DOCKER_INDEX.md`, `docs/DOCKER_COMMANDS.md`, and `docs/DOCKER_ARCHITECTURE.md`.
  - Documented actual Nginx routes:
    - `/` -> `tasksense-client:5173`
    - `/api/core/v1` and `/api/core/v1/*` -> `tasksense-spring-api:8080/api/v1`
    - `/api/chat/v1` and `/api/chat/v1/*` -> `tasksense-ai:8000/api/v1`
  - Removed old command patterns using root `docker-compose` and stale service names such as `spring`, `ai`, `frontend`, `postgres`, and `nginx`.
  - Documented internal-only health checks for AI, Elasticsearch, Qdrant, Redis, and PostgreSQL.
  - Documented that current compose publishes only host port `80`.
  - Added Kubernetes path mapping notes based on the actual Nginx route behavior.
- Files touched:
  - `docs/DOCKER_SUMMARY.md`
  - `docs/DOCKER_SETUP.md`
  - `docs/DOCKER_INDEX.md`
  - `docs/DOCKER_COMMANDS.md`
  - `docs/DOCKER_ARCHITECTURE.md`
  - `docs/ai-context/runs/RUN-2026-06-05-005.md`
  - `docs/ai-context/INDEX.md`
  - `docs/ai-context/LEDGER.md`
- Commands/tests run:
  - `rg` sweep for stale Docker/Nginx route references in `docs/DOCKER_*.md`
  - `git diff -- infra/docker/compose.yaml`
  - `docker compose --env-file .env.example -f infra/docker/compose.yaml config --quiet`
  - `IMAGE_PREFIX=ghcr.io/example/tasksense IMAGE_TAG=test docker compose --env-file .env.example -f infra/docker/compose.yaml -f infra/docker/compose.build.yaml config --quiet`

## Quality and Security

- Requirement compliance result: Docker docs now match the current Nginx config and compose service names.
- Plan compliance result: Completed documentation updates and validation without Docker Compose edits.
- Security check result: Real `.env` was not read or modified.
- Release readiness result: Compose config validation passes; only documentation changed.

## Follow-ups

- Remaining issues:
  - If HTTPS, `/health`, or direct host ports are required, they need an explicit future infra change.
  - If production DB password must change, a compose override or infra change is still required because base compose hardcodes several DB consumers.
- Next actions:
  - Keep `proxy/nginx.conf` as the source of truth whenever Docker docs mention public routes.
- Context promoted to ledger:
  - Docker docs are aligned to `proxy/nginx.conf`; Nginx public routes are `/`, `/api/core/v1`, and `/api/chat/v1`; no Compose edits were made.
