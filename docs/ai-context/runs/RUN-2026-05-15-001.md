# RUN-2026-05-15-001

## Summary

- Added backend observability stage 3 support: Spring Actuator exposure for health/metrics/prometheus and Micrometer Prometheus registry dependency.
- Added custom wrapped `/api/v1/health` API with service-layer status from Spring application availability and non-sensitive endpoint hints.
- Added Micrometer counter/timer metrics for custom health API calls and unit tests for controller/service behavior.

## Validation

- `./mvnw test -Dtest=HealthServiceImplTest,HealthControllerTest` failed because `server/mvnw` is not executable in this checkout.
- `mvn test -Dtest=HealthServiceImplTest,HealthControllerTest` failed because `mvn` is not installed.
- `sh ./mvnw test -Dtest=HealthServiceImplTest,HealthControllerTest` passed: 3 tests, 0 failures, 0 errors, 0 skipped.

## Notes

- Actuator health details remain hidden via `management.endpoint.health.show-details=never`.
- Prometheus scrape endpoint is available at `/api/v1/actuator/prometheus` because the app uses servlet context path `/api/v1`.
