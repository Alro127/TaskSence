# Run Record

## Run Meta

- Run ID: RUN-2026-05-12-001
- Date: 2026-05-12
- Trigger: User requested improving Agent Mode/MCP after RAG completion, including Spring-supported Java MCP tools and prompt/context/reranking follow-up work.
- Request Summary: Research current Spring AI MCP approach with subagents, review TaskSense AI/MCP architecture, inspect current code, and begin Phase 1 by adding official Spring AI MCP tool facade for core creation actions.
- Primary Skill(s): workflow.run, feature-implementation, context-doc-sync
- Agent Role(s): librarian research, oracle architecture review, explorer code mapping, fixer implementation planning, backend MCP implementation

## Inputs Snapshots

- Key files reviewed:
  - `AGENTS.md`
  - `.github/copilot-instructions.md`
  - `docs/ai-context/INDEX.md`
  - `docs/ai-context/LEDGER.md`
  - latest run records in `docs/ai-context/runs/`
  - `server/pom.xml`
  - `server/src/main/resources/application.yaml`
  - `server/src/main/java/dev/alro127/tasksense/service/impl/McpServiceImpl.java`
  - backend task/project/workspace request DTOs and service interfaces
- Constraints detected:
  - Existing custom `/mcp/execute` endpoint remains for compatibility during migration.
  - Java MCP dependency and application config already exist, but no Spring AI tool facade was present.
  - Backend validation initially could not run because `JAVA_HOME` was not configured; after Java setup, compile passed.
- Assumptions made:
  - Spring AI `@Tool` annotation package is available from the configured Spring AI MCP starter.
  - Initial MCP migration should cover safe core create tools before workflow generation/import.

## Plan and Validation

- Planning approach:
  - Use subagent research/review/exploration before code edits.
  - Keep Phase 1 scoped to official MCP tool facade for `create_task`, `create_project`, and `create_workspace`.
  - Reuse existing domain services and permission checks instead of direct repository writes.
- Plan reviewer findings:
  - Main blocker remains AI-to-Java auth/delegated identity design.
  - Workflow generation/import should wait for a clearer domain contract.
- Design compliance checks:
  - Backend layering is preserved by routing tools through `TaskService`, `ProjectService`, and `WorkspaceService`.
  - Existing API wrappers and custom `/mcp/execute` compatibility endpoint were not changed.

## Work Completed

- Changes made:
  - Added `TaskSenseMcpTools` Spring service with Spring AI `@Tool` methods:
    - `create_task`
    - `create_project`
    - `create_workspace`
  - Added typed nested tool request classes for core create operations.
  - Reused existing DTOs and domain services for execution.
  - Enforced project/workspace permissions before creating tasks/projects.
  - Added concise MCP tool audit logs for created resource IDs.
  - Extended compatibility `/mcp/execute` dispatcher with `create_workspace` while Python migration is still in progress.
  - Forwarded the authenticated user JWT from AI controller to the Agent MCP client for compatibility calls.
  - Expanded Agent Mode planner prompt and allowed actions for `create_workspace` plus stricter required-field checks.
  - Added deterministic RAG reranking signals in `filter_docs` for SQL-template priority, entity-type intent boosts, query-token matches, due-date boost, and done-task downrank.
  - Reverted the experimental workflow-to-project implementation to avoid changing existing workflow logic.
  - Added workflow-related MCP action `create_workflow_from_project` that wraps the existing stable `WorkflowService.createWorkflowFromProject(...)` flow.
  - Added Agent reasoning payload end-to-end:
    - Planner emits concise reasoning steps.
    - AI response includes `reasoning` for current turn.
    - Chat persistence stores `agentReasoning` in assistant message context.
    - Frontend chat renders an "AI reasoning" panel on assistant messages.
  - Added official MCP Streamable HTTP client mode in `ai/app/agent/mcp_client.py`:
    - Supports `initialize`, `notifications/initialized`, `tools/list`, and `tools/call` JSON-RPC requests.
    - Uses `SPRING_MCP_TRANSPORT=auto` by default to try official MCP first and falls back to compatibility `/execute`.
    - Supports `SPRING_MCP_TRANSPORT=streamable` to force official MCP transport and `SPRING_MCP_TRANSPORT=compat` to force old REST compatibility mode.
  - Added SiliconFlow provider support for LangChain chat and embeddings:
    - `LLM_PROVIDER=siliconflow` uses `ChatOpenAI` with `SILICONFLOW_BASE_URL` defaulting to `https://api.siliconflow.cn/v1`.
    - Default generator is `deepseek-ai/DeepSeek-V3`; Recommended alternatives include `deepseek-ai/DeepSeek-R1`, `Qwen/Qwen2.5-72B-Instruct`, and free-tier `deepseek-ai/DeepSeek-V2.5` when available on the account.
    - Embeddings use SiliconFlow `/embeddings` through the existing OpenAI-compatible embedding client path, defaulting to `BAAI/bge-m3`.
    - Added settings: `SILICONFLOW_API_KEY`, `SILICONFLOW_MODEL`, `SILICONFLOW_EMBEDDING_MODEL`, `SILICONFLOW_BASE_URL`.
- Files touched:
  - `server/src/main/java/dev/alro127/tasksense/mcp/TaskSenseMcpTools.java`
  - `server/src/main/java/dev/alro127/tasksense/service/impl/McpServiceImpl.java`
  - `ai/app/agent/mcp_client.py`
  - `ai/app/agent/orchestrator.py`
  - `ai/app/auth/dependencies.py`
  - `ai/app/controller/v1/chatbot_controller.py`
  - `ai/app/service/chat_persistence_service.py`
  - `ai/app/service/chatbot/pipeline.py`
  - `ai/app/chatbot/components/filter.py`
  - `ai/app/chatbot/prompts/agent_action_planner.txt`
  - `client/src/types/api.ts`
  - `client/src/features/agent/components/types.ts`
  - `client/src/features/agent/components/ChatConversation.tsx`
  - `client/src/features/agent/pages/AgentChatPage.tsx`
  - `ai/pyproject.toml`
  - `ai/uv.lock`
  - `docs/ai-context/runs/RUN-2026-05-12-001.md`
- Commands/tests run:
  - `./mvnw -DskipTests compile` from `server/` failed because `./mvnw` is not executable in this checkout.
  - `bash ./mvnw -DskipTests compile` from `server/` initially failed because `JAVA_HOME` was not configured.
  - `bash ./mvnw -DskipTests compile` from `server/` passed after Java setup.
  - `bash ./mvnw -DskipTests compile` from `server/` passed after compatibility MCP updates.
  - `python3 -m py_compile app/agent/mcp_client.py app/agent/orchestrator.py app/service/chatbot/pipeline.py app/controller/v1/chatbot_controller.py app/auth/dependencies.py app/chatbot/components/filter.py` from `ai/` passed.
  - `uv run --with pyright ...` could not run because `uv` is not installed in this environment.
  - `bash ./mvnw -DskipTests compile` from `server/` passed after adding workflow MCP wrapper without changing workflow logic.
  - `python3 -m py_compile app/agent/mcp_client.py app/agent/orchestrator.py app/service/chatbot/pipeline.py app/controller/v1/chatbot_controller.py app/auth/dependencies.py app/service/chat_persistence_service.py` from `ai/` passed after reasoning changes.
  - `npm install` from `client/` restored frontend dependencies; npm reported 11 vulnerabilities (6 moderate, 5 high) for later audit.
  - `npx eslint src/features/agent/api/agentApi.ts src/features/agent/pages/AgentChatPage.tsx src/features/agent/components/ChatConversation.tsx src/features/agent/components/types.ts src/types/api.ts` from `client/` passed.
  - `bash ./mvnw -DskipTests compile` from `server/` passed on recheck.
  - `python3 -m py_compile app/agent/mcp_client.py app/agent/orchestrator.py app/service/chatbot/pipeline.py app/controller/v1/chatbot_controller.py app/auth/dependencies.py app/service/chat_persistence_service.py app/chatbot/components/filter.py` from `ai/` passed on recheck.
  - `python3 -m py_compile ...` from `ai/` passed after adding official MCP Streamable HTTP client mode.
  - `bash ./mvnw -DskipTests compile` from `server/` passed after MCP client/backend wrapper recheck.
  - `python3 -m py_compile app/config/config.py app/client/llms.py app/client/embedding.py` from `ai/` passed after SiliconFlow integration.

## Quality and Security

- Requirement compliance result: PARTIAL PASS for Phase 1/2/3 foundation; core Spring AI MCP tool facade, compatibility create-workspace action, JWT forwarding, planner guardrails, deterministic reranking, workflow MCP wrapper over existing stable logic, and visible UI reasoning were added, but official Python MCP transport, richer generated workflow creation, and final auth model remain follow-ups.
- Plan compliance result: PASS for scoped Phase 1 changes.
- Security check result: PARTIAL PASS; tool methods keep server-side permission checks, but service-to-service/user JWT propagation is still an open architecture task.
- Release readiness result: LIMITED GO for the scoped Java compile gate; still NO-GO for full Agent Mode release until MCP discovery/e2e tool-call tests and AI auth propagation are implemented.

## Follow-ups

- Remaining issues:
  - Add runtime MCP tool discovery checks against the configured Spring AI MCP endpoint.
  - Decide and implement AI service -> Java MCP auth propagation.
  - Runtime-test official MCP Streamable HTTP tool discovery/calls with Spring app running.
  - Add richer generated workflow support only by reusing/extending existing workflow logic safely; do not replace current working workflow behavior.
  - Add RAG evaluation fixtures and optionally model/cross-encoder reranking behind a config flag.
- Next actions:
  - Add focused backend tests for MCP tool facade permissions and success paths.
  - Verify MCP endpoint tool discovery/listing once the app can run with local infrastructure.
  - Switch `SPRING_MCP_TRANSPORT` from `auto` to `streamable` after runtime MCP discovery/e2e tests pass.
  - Runtime-test SiliconFlow chat and embedding calls with a real `SILICONFLOW_API_KEY`.
- Context promoted to leader:
  - Spring AI MCP migration has started by adding an official MCP tool facade while retaining the old REST dispatcher as compatibility-only.
  - Workflow-related MCP action now wraps the existing stable project-to-workflow draft creation flow instead of changing workflow semantics.
  - SiliconFlow is available as a LangChain chat/embedding provider via OpenAI-compatible API settings.