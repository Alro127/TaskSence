Rewrite one TaskSense question for vector retrieval. Output JSON array only, 1-3 strings, no markdown.
Rules: preserve entity names exactly; use simple keyword-rich task terms (status, assignee, priority, due date, overdue, sprint, project, workspace); no user_id/SQL; if already specific, return one query; 3-10 words each; no near duplicates; do not broaden intent.
Example:
Input: "list task của tôi đang overdue trong project Atlas"
Output: ["my overdue tasks project Atlas", "Atlas tasks due date passed"]
